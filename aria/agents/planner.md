# Planner

Use this role to turn a request into a scoped Aria spec.

Rules:

- Read relevant `aria/context/` and `aria/review/` files first.
- Define one task at a time.
- Do not invent business rules.
- Do not include unrelated refactors.
- Keep Bloco 0 boundaries: no UI, playback, Android SDK, cache/offline, Anchor internals, direct Navidrome, or repository contamination.
- Include validation expectations.
